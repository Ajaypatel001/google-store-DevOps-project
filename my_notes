COMPLETE COMMAND NOTES (DevOps Project)
🔹 1. AWS CLI Commands
aws configure
aws sts get-caller-identity
aws eks list-clusters
aws eks describe-cluster --name naresh --region us-east-1
aws eks --region us-east-1 update-kubeconfig --name naresh
🔹 2. Terraform Commands
terraform init
terraform plan
terraform apply
terraform apply -auto-approve
terraform destroy
terraform destroy -auto-approve
🔹 3. Git Commands
git clone <repo-url>
cd <project-folder>

git add .
git commit -m "message"
git push

git remote add origin <repo-url>
git push -u origin main

git status
🔹 4. Docker Commands (Local use)
docker build -t my-app .
docker tag my-app <ecr-url>
docker push <ecr-url>
🔹 5. Kubernetes (kubectl) Commands
📌 Basic
kubectl get nodes
kubectl get pods
kubectl get svc
kubectl get all
📌 Deploy
kubectl apply -f backend-deployment.yml
kubectl apply -f backend-service.yml

kubectl apply -f frontend-deployment.yml
kubectl apply -f frontend-service.yml
📌 Delete
kubectl delete -f backend-deployment.yml
kubectl delete -f frontend-deployment.yml
📌 Debug
kubectl describe pod <pod-name>
kubectl logs <pod-name>
🔹 6. Argo CD Commands
kubectl create namespace argocd

kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

kubectl get pods -n argocd

kubectl port-forward svc/argocd-server -n argocd 8080:443
🔑 Argo CD Login Password
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d
🔹 7. GitHub Actions Trigger
git add .
git commit -m "trigger pipeline"
git push
📌 Empty commit (restart pipeline)
git commit --allow-empty -m "trigger pipeline"
git push
🔹 8. Check LoadBalancer
kubectl get svc

👉 Output:

EXTERNAL-IP
🔹 9. System Commands (Linux / EC2)
yum update -y
curl -LO <url>
chmod +x file
mv file /usr/local/bin/
🔹 10. Important Flow Commands
aws eks update-kubeconfig
kubectl get nodes
kubectl apply -f .
kubectl get pods
kubectl get svc
🎯 FINAL FLOW (YAAD RAKHNE KE LIYE)
AWS configure
→ Terraform apply
→ EKS cluster create
→ GitHub push
→ GitHub Actions run
→ Docker build
→ ECR push
→ kubectl deploy
→ Argo CD sync
→ App live 🚀
🔥 Ajay ji (important tip)

👉 Viva me bolna:

"I used Terraform for infrastructure, GitHub Actions for CI/CD, and Argo CD for GitOps deployment."
